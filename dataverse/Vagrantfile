Vagrant.configure("2") do |config|
  config.vm.box = "ubuntu/jammy64"
  config.vm.provision :shell, 
  path: "dataverse.build.sh",
  keep_color: true
  config.vm.synced_folder "..", "/demo_repository_deployments", :mount_options => ["dmode=777","fmode=777"]
  config.vm.network :forwarded_port, host: 99, guest: 80
  config.vm.network :forwarded_port, host: 9999, guest: 8080
  config.vbguest.auto_update = false
  config.vm.define 'dataverse' do |t|
    config.vm.provider :virtualbox do |v|
      v.customize ["modifyvm", :id, "--memory", 4096]
    end
  end
end
